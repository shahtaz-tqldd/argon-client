import { useMemo, useState } from "react";
import {
  ArrowDown, ArrowLeft, ArrowUp, BellRing, Building2, CalendarCheck2,
  CalendarClock, CalendarDays, CalendarRange, CalendarX2, Check, ChevronRight,
  Clock3, Download, ExternalLink, Facebook, Globe2, Instagram, Link2, Mail,
  MapPin, MessageCircleMore, Pencil, Phone, Plus, Search, Settings2, Sparkles,
  UserRound, UserRoundCheck, Video, X,
} from "lucide-react";
import { Link } from "react-router-dom";
import { toast } from "sonner";

import ReusableTable from "@/components/table";
import { Button } from "@/components/ui/button";
import Card from "@/components/ui/card";
import { Checkbox } from "@/components/ui/checkbox";
import {
  Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader,
  DialogTitle,
} from "@/components/ui/dialog";
import { FloatingInput, Input } from "@/components/ui/input";
import {
  FloatingSelect, Select, SelectContent, SelectItem, SelectTrigger, SelectValue,
} from "@/components/ui/select";
import TabMenu from "@/components/ui/tab";
import { FloatingTextarea } from "@/components/ui/textarea";
import { cn, getInitials } from "@/lib/utils";

import { initialAppointments, initialBookingFields, initialSchedule } from "./demo-data";

const pageTabs = [
  { value: "appointments", label: "Appointments", icon: CalendarDays, count: 6 },
  { value: "availability", label: "Availability", icon: CalendarRange },
  { value: "settings", label: "Settings", icon: Settings2 },
];

const channelMeta = {
  Website: { icon: Globe2, tone: "bg-sky-500/10 text-sky-600" },
  WhatsApp: { icon: MessageCircleMore, tone: "bg-emerald-500/10 text-emerald-600" },
  Instagram: { icon: Instagram, tone: "bg-fuchsia-500/10 text-fuchsia-600" },
  Facebook: { icon: Facebook, tone: "bg-blue-600/10 text-blue-600" },
};
const statusStyles = {
  Confirmed: "bg-emerald-500/10 text-emerald-600 dark:text-emerald-400",
  Pending: "bg-amber-500/10 text-amber-700 dark:text-amber-400",
  Completed: "bg-primary/10 text-primary",
  Cancelled: "bg-muted text-muted-foreground",
};

function Toggle({ checked, onChange, disabled = false }) {
  return <button type="button" role="switch" aria-checked={checked} disabled={disabled} onClick={() => onChange(!checked)} className={cn("relative h-6 w-11 shrink-0 rounded-full transition", checked ? "bg-primary" : "bg-muted-foreground/25", disabled && "cursor-not-allowed opacity-50")}><span className={cn("absolute top-0.5 size-5 rounded-full bg-white shadow transition", checked ? "left-[22px]" : "left-0.5")} /></button>;
}

function ChannelIcon({ source }) {
  const meta = channelMeta[source] || channelMeta.Website;
  const Icon = meta.icon;
  return <span className={cn("flex size-6 items-center justify-center rounded-full", meta.tone)}><Icon className="size-3" /></span>;
}

function AppointmentIdentity({ appointment }) {
  return <div className="flex min-w-52 items-center gap-3"><span className="flex size-10 shrink-0 items-center justify-center rounded-full bg-primary/10 text-xs font-bold text-primary">{getInitials(appointment.name)}</span><div className="min-w-0"><p className="truncate text-sm font-semibold text-foreground">{appointment.name}</p><p className="mt-0.5 truncate text-xs text-muted-foreground">{appointment.email}</p></div></div>;
}

function AppointmentStatus({ status }) {
  return <span className={cn("inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-xs font-semibold", statusStyles[status])}><span className="size-1.5 rounded-full bg-current" />{status}</span>;
}

function MetricCard({ icon, label, value, detail, tone, badge }) {
  const MetricIcon = icon;
  return <Card className="p-5"><div className="flex items-center justify-between"><span className={cn("flex size-9 items-center justify-center rounded-xl", tone)}><MetricIcon className="size-4" /></span>{badge}</div><p className="mt-4 text-2xl font-bold">{value}</p><p className="mt-1 text-xs font-medium text-muted-foreground">{label}</p><p className="mt-3 text-[11px] text-muted-foreground">{detail}</p></Card>;
}

function DetailRow({ icon, label, value }) {
  const DetailIcon = icon;
  return <div className="flex gap-3"><DetailIcon className="mt-0.5 size-4 shrink-0 text-muted-foreground" /><div className="min-w-0"><p className="text-[10px] uppercase tracking-wide text-muted-foreground">{label}</p><p className="mt-0.5 truncate text-xs font-medium">{value}</p></div></div>;
}

function AppointmentDialog({ appointment, onClose, onStatusChange }) {
  if (!appointment) return null;
  return <Dialog open onOpenChange={(open) => !open && onClose()}><DialogContent className="custom-scrollbar max-h-[90vh] overflow-y-auto rounded-3xl p-0 sm:max-w-3xl"><DialogHeader className="border-b bg-muted/30 px-6 py-6"><div className="flex items-center gap-4"><span className="flex size-12 items-center justify-center rounded-full bg-primary/10 text-sm font-bold text-primary">{getInitials(appointment.name)}</span><div><div className="flex flex-wrap items-center gap-2"><DialogTitle>{appointment.title}</DialogTitle><AppointmentStatus status={appointment.status} /></div><DialogDescription className="mt-1">Booked by {appointment.name} · {appointment.company}</DialogDescription></div></div></DialogHeader><div className="grid md:grid-cols-[0.9fr_1.25fr]"><section className="border-b p-6 md:border-b-0 md:border-r"><p className="text-xs font-bold uppercase tracking-wide text-muted-foreground">Guest details</p><div className="mt-4 space-y-4">{[[UserRound, "Name", appointment.name], [Mail, "Email", appointment.email], [Phone, "Phone", appointment.phone], [Building2, "Company", appointment.company]].map(([icon, label, value]) => <DetailRow key={label} icon={icon} label={label} value={value} />)}</div><div className="mt-6"><FloatingSelect label="Appointment status" value={appointment.status} displayValue={appointment.status} onValueChange={(value) => onStatusChange(appointment.id, value)}>{["Pending", "Confirmed", "Completed", "Cancelled"].map((item) => <SelectItem key={item} value={item}>{item}</SelectItem>)}</FloatingSelect></div></section><div><section className="border-b p-6"><p className="text-xs font-bold uppercase tracking-wide text-muted-foreground">Schedule</p><div className="mt-4 grid gap-4 sm:grid-cols-2">{[[CalendarDays, "Date", appointment.date], [Clock3, "Time", `${appointment.time} · ${appointment.timezone}`], [CalendarClock, "Duration", appointment.duration], [UserRoundCheck, "Host", appointment.host], [Video, "Location", appointment.location], [Globe2, "Source", appointment.source]].map(([icon, label, value]) => <DetailRow key={label} icon={icon} label={label} value={value} />)}</div><Button variant="outline" size="sm" className="mt-5"><ExternalLink />Open calendar event</Button></section><section className="border-b p-6"><div className="flex items-center gap-2"><Sparkles className="size-4 text-primary" /><p className="text-xs font-bold uppercase tracking-wide text-muted-foreground">Booking context</p></div><p className="mt-3 text-sm leading-6">{appointment.notes}</p></section><section className="flex flex-wrap gap-2 p-6"><Button size="sm"><CalendarClock />Reschedule</Button><Button size="sm" variant="outline"><Mail />Email guest</Button>{appointment.status !== "Cancelled" && <Button size="sm" variant="ghost" className="text-destructive" onClick={() => onStatusChange(appointment.id, "Cancelled")}><X />Cancel appointment</Button>}</section></div></div></DialogContent></Dialog>;
}

function AppointmentsTab({ appointments, setAppointments, openAppointment }) {
  const [query, setQuery] = useState("");
  const [status, setStatus] = useState("all");
  const [page, setPage] = useState(1);
  const [pageSize, setPageSize] = useState(10);
  const visible = useMemo(() => appointments.filter((item) => `${item.name} ${item.email} ${item.company} ${item.title}`.toLowerCase().includes(query.toLowerCase()) && (status === "all" || item.status.toLowerCase() === status)), [appointments, query, status]);
  const rows = visible.slice((page - 1) * pageSize, page * pageSize).map((item) => ({ id: item.id, raw: item, guest: <AppointmentIdentity appointment={item} />, schedule: <div className="min-w-36"><p className="text-xs font-semibold text-foreground">{item.date}</p><p className="mt-1 text-[11px] text-muted-foreground">{item.time} · {item.timezone}</p></div>, appointmentType: <div><p className="text-xs font-semibold text-foreground">{item.title}</p><p className="mt-1 text-[11px] text-muted-foreground">{item.duration}</p></div>, host: <div><p className="text-xs font-medium text-foreground">{item.host}</p><div className="mt-1 flex items-center gap-1.5"><ChannelIcon source={item.source} /><span className="text-[11px] text-muted-foreground">{item.source}</span></div></div>, appointmentStatus: <AppointmentStatus status={item.status} />, booked: <div><p className="text-xs font-medium text-foreground">{item.booked}</p><p className="mt-1 text-[11px] text-muted-foreground">{item.location}</p></div>, action: "" }));
  const update = (id, nextStatus) => { setAppointments((current) => current.map((item) => item.id === id ? { ...item, status: nextStatus } : item)); toast.success(`Appointment marked ${nextStatus.toLowerCase()}`); };
  return <div className="space-y-5"><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><MetricCard icon={CalendarCheck2} label="Upcoming" value="18" detail="Across the next 7 days" tone="bg-primary/10 text-primary" badge={<span className="text-xs font-semibold text-emerald-600">+4 this week</span>} /><MetricCard icon={Clock3} label="Today" value="4" detail="Next at 2:30 PM" tone="bg-amber-500/10 text-amber-600" badge={<span className="size-2 rounded-full bg-amber-500" />} /><MetricCard icon={UserRoundCheck} label="Completed" value="42" detail="93% attendance rate" tone="bg-emerald-500/10 text-emerald-600" badge={<span className="text-xs font-semibold text-emerald-600">+12%</span>} /><MetricCard icon={CalendarX2} label="Cancelled" value="3" detail="6.2% cancellation rate" tone="bg-red-500/10 text-red-600" badge={<span className="text-xs font-semibold text-muted-foreground">This month</span>} /></div><ReusableTable title="Booked appointments" description={`${visible.length} matching appointments · Times shown in visitor timezone`} headerActions={<div className="flex flex-wrap items-center gap-2"><label className="relative hidden md:block"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><Input value={query} onChange={(event) => { setQuery(event.target.value); setPage(1); }} className="h-9 w-56 rounded-xl bg-slate-50 pl-9" placeholder="Search appointments" /></label><Select value={status} onValueChange={(value) => { setStatus(value); setPage(1); }}><SelectTrigger className="h-9 w-36 rounded-xl bg-slate-50"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="all">All statuses</SelectItem>{["Confirmed", "Pending", "Completed", "Cancelled"].map((item) => <SelectItem key={item} value={item.toLowerCase()}>{item}</SelectItem>)}</SelectContent></Select></div>} data={rows} columns={[{ header: "Guest", accessorKey: "guest" }, { header: "Date & time", accessorKey: "schedule" }, { header: "Appointment", accessorKey: "appointmentType" }, { header: "Host & source", accessorKey: "host" }, { header: "Status", accessorKey: "appointmentStatus" }, { header: "Booked", accessorKey: "booked" }, { header: "", accessorKey: "action" }]} isLoading={false} totalItems={visible.length} page={page} setPage={setPage} pageSize={pageSize} setPageSize={setPageSize} table_options={[{ label: "View details", action: (_, row) => openAppointment(row.raw) }, { label: "Reschedule", action: (_, row) => toast.success(`Reschedule link opened for ${row.raw.name}`) }, { label: "Mark completed", hidden: (row) => ["Completed", "Cancelled"].includes(row.raw.status), action: (_, row) => update(row.id, "Completed") }, { label: "Cancel appointment", hidden: (row) => row.raw.status === "Cancelled", action: (_, row) => update(row.id, "Cancelled") }]} onDeleteConfirm={async () => {}} deleteLoading={false} emptyTitle="No appointments found" emptyDescription="Try changing your search or status filter." /></div>;
}

function SectionCard({ icon, title, description, action, children, className }) {
  const SectionIcon = icon;
  return <Card className={cn("p-0", className)}><div className="flex items-start justify-between gap-4 border-b p-5"><div className="flex gap-3"><span className="flex size-10 shrink-0 items-center justify-center rounded-2xl bg-primary/10 text-primary"><SectionIcon className="size-5" /></span><div><h2 className="text-sm font-bold">{title}</h2><p className="mt-1 text-xs leading-5 text-muted-foreground">{description}</p></div></div>{action}</div><div className="p-5">{children}</div></Card>;
}

function AvailabilityDialog({ open, onClose, schedule, setSchedule }) {
  const [draft, setDraft] = useState(schedule);
  const save = () => { setSchedule(draft); toast.success("Weekly availability updated"); onClose(); };
  return <Dialog open={open} onOpenChange={(next) => !next && onClose()}><DialogContent className="custom-scrollbar max-h-[92vh] overflow-y-auto rounded-3xl p-0 sm:max-w-2xl"><DialogHeader className="border-b bg-muted/30 px-6 py-6"><DialogTitle>Edit weekly availability</DialogTitle><DialogDescription>Choose the days and hours when visitors can book through the chatbot.</DialogDescription></DialogHeader><div className="space-y-2 px-6 py-6">{draft.map((day) => <div key={day.id} className={cn("grid grid-cols-[110px_minmax(0,1fr)_auto] items-center gap-3 rounded-2xl border p-3", !day.enabled && "opacity-55")}><div className="flex items-center gap-2"><Toggle checked={day.enabled} onChange={(value) => setDraft((current) => current.map((item) => item.id === day.id ? { ...item, enabled: value } : item))} /><span className="text-xs font-semibold">{day.day}</span></div>{day.enabled ? <div className="flex items-center gap-2"><Input value={day.start} onChange={(event) => setDraft((current) => current.map((item) => item.id === day.id ? { ...item, start: event.target.value } : item))} className="h-9 rounded-xl text-center text-xs" /><span className="text-xs text-muted-foreground">to</span><Input value={day.end} onChange={(event) => setDraft((current) => current.map((item) => item.id === day.id ? { ...item, end: event.target.value } : item))} className="h-9 rounded-xl text-center text-xs" /></div> : <p className="text-xs text-muted-foreground">Unavailable</p>}<button className="text-muted-foreground hover:text-primary"><Plus className="size-4" /></button></div>)}</div><DialogFooter className="border-t bg-muted/20 px-6 py-4"><Button variant="outline" onClick={onClose}>Cancel</Button><Button onClick={save}><Check />Save availability</Button></DialogFooter></DialogContent></Dialog>;
}

function RulesDialog({ open, onClose, rules, setRules }) {
  const [draft, setDraft] = useState(rules);
  const save = () => { setRules(draft); toast.success("Booking rules saved"); onClose(); };
  return <Dialog open={open} onOpenChange={(next) => !next && onClose()}><DialogContent className="rounded-3xl p-0 sm:max-w-xl"><DialogHeader className="border-b bg-muted/30 px-6 py-6"><DialogTitle>Booking rules</DialogTitle><DialogDescription>Control duration, buffers, booking windows, and timezone.</DialogDescription></DialogHeader><div className="grid gap-5 px-6 py-6 sm:grid-cols-2"><FloatingSelect label="Default duration" value={draft.duration} displayValue={draft.duration} onValueChange={(value) => setDraft((current) => ({ ...current, duration: value }))}>{["15 minutes", "30 minutes", "45 minutes", "60 minutes"].map((item) => <SelectItem key={item} value={item}>{item}</SelectItem>)}</FloatingSelect><FloatingSelect label="Buffer between calls" value={draft.buffer} displayValue={draft.buffer} onValueChange={(value) => setDraft((current) => ({ ...current, buffer: value }))}>{["No buffer", "10 minutes", "15 minutes", "30 minutes"].map((item) => <SelectItem key={item} value={item}>{item}</SelectItem>)}</FloatingSelect><FloatingSelect label="Minimum notice" value={draft.notice} displayValue={draft.notice} onValueChange={(value) => setDraft((current) => ({ ...current, notice: value }))}>{["1 hour", "4 hours", "12 hours", "24 hours"].map((item) => <SelectItem key={item} value={item}>{item}</SelectItem>)}</FloatingSelect><FloatingSelect label="Booking window" value={draft.window} displayValue={draft.window} onValueChange={(value) => setDraft((current) => ({ ...current, window: value }))}>{["14 days", "30 days", "60 days", "90 days"].map((item) => <SelectItem key={item} value={item}>{item}</SelectItem>)}</FloatingSelect><div className="sm:col-span-2"><FloatingSelect label="Schedule timezone" value={draft.timezone} displayValue={draft.timezone} onValueChange={(value) => setDraft((current) => ({ ...current, timezone: value }))}>{["Asia/Dhaka (UTC+6)", "America/Los_Angeles (UTC−7)", "America/New_York (UTC−4)", "Europe/London (UTC+1)"].map((item) => <SelectItem key={item} value={item}>{item}</SelectItem>)}</FloatingSelect></div><div className="flex items-start justify-between gap-4 rounded-2xl border p-4 sm:col-span-2"><div><p className="text-sm font-semibold">Visitor timezone detection</p><p className="mt-1 text-xs text-muted-foreground">Show available times in each visitor’s local timezone.</p></div><Toggle checked={draft.detectTimezone} onChange={(value) => setDraft((current) => ({ ...current, detectTimezone: value }))} /></div></div><DialogFooter className="border-t bg-muted/20 px-6 py-4"><Button variant="outline" onClick={onClose}>Cancel</Button><Button onClick={save}><Check />Save rules</Button></DialogFooter></DialogContent></Dialog>;
}

function AvailabilityTab({ schedule, setSchedule }) {
  const [scheduleOpen, setScheduleOpen] = useState(false);
  const [rulesOpen, setRulesOpen] = useState(false);
  const [rules, setRules] = useState({ duration: "30 minutes", buffer: "15 minutes", notice: "4 hours", window: "30 days", timezone: "Asia/Dhaka (UTC+6)", detectTimezone: true });
  return <div className="grid items-start gap-5 lg:grid-cols-[minmax(0,1.2fr)_minmax(320px,0.8fr)]"><SectionCard icon={CalendarRange} title="Weekly schedule" description="The recurring hours visitors can choose from." action={<Button variant="ghost" size="icon-sm" onClick={() => setScheduleOpen(true)}><Pencil /></Button>}><div className="divide-y">{schedule.map((day) => <div key={day.id} className="flex items-center justify-between gap-4 py-3"><div className="flex items-center gap-3"><span className={cn("size-2 rounded-full", day.enabled ? "bg-emerald-500" : "bg-muted-foreground/30")} /><span className="w-20 text-xs font-semibold">{day.day}</span></div>{day.enabled ? <div className="flex items-center gap-2 text-xs"><span className="rounded-lg bg-muted px-2.5 py-1.5 font-medium">{day.start}</span><span className="text-muted-foreground">–</span><span className="rounded-lg bg-muted px-2.5 py-1.5 font-medium">{day.end}</span></div> : <span className="text-xs text-muted-foreground">Unavailable</span>}</div>)}</div></SectionCard><div className="space-y-5"><SectionCard icon={CalendarClock} title="Booking rules" description="Timing, notice, buffers, and timezone." action={<Button variant="ghost" size="icon-sm" onClick={() => setRulesOpen(true)}><Pencil /></Button>}><div className="divide-y">{[["Default duration", rules.duration], ["Buffer", rules.buffer], ["Minimum notice", rules.notice], ["Book up to", rules.window], ["Timezone", rules.timezone]].map(([label, value]) => <div key={label} className="flex justify-between gap-4 py-2.5 text-xs"><span className="text-muted-foreground">{label}</span><span className="text-right font-semibold">{value}</span></div>)}</div></SectionCard><SectionCard icon={Video} title="Appointment types" description="Different meeting options offered by the chatbot." action={<Button variant="ghost" size="icon-sm"><Plus /></Button>}><div className="space-y-2">{[["Product demo", "30 min", "bg-primary"], ["Sales consultation", "45 min", "bg-violet-500"], ["Onboarding call", "30 min", "bg-emerald-500"]].map(([name, duration, color]) => <button key={name} className="flex w-full items-center justify-between rounded-xl border p-3 text-left transition hover:bg-muted/40"><span className="flex items-center gap-2.5 text-xs font-semibold"><span className={cn("size-2 rounded-full", color)} />{name}</span><span className="text-[11px] text-muted-foreground">{duration}</span></button>)}</div></SectionCard></div><AvailabilityDialog open={scheduleOpen} onClose={() => setScheduleOpen(false)} schedule={schedule} setSchedule={setSchedule} /><RulesDialog open={rulesOpen} onClose={() => setRulesOpen(false)} rules={rules} setRules={setRules} /></div>;
}

function BookingFieldsDialog({ open, onClose, fields, setFields }) {
  const [draft, setDraft] = useState(fields);
  const update = (id, key, value) => setDraft((current) => current.map((field) => field.id === id ? { ...field, [key]: value } : field));
  const move = (index, direction) => { const target = index + direction; if (target < 0 || target >= draft.length) return; setDraft((current) => { const next = [...current]; [next[index], next[target]] = [next[target], next[index]]; return next; }); };
  const save = () => { setFields(draft); toast.success("Booking form fields saved"); onClose(); };
  return <Dialog open={open} onOpenChange={(next) => !next && onClose()}><DialogContent className="custom-scrollbar max-h-[90vh] overflow-y-auto rounded-3xl p-0 sm:max-w-2xl"><DialogHeader className="border-b bg-muted/30 px-6 py-6"><DialogTitle>Booking information</DialogTitle><DialogDescription>Choose what the chatbot should collect before confirming an appointment.</DialogDescription></DialogHeader><div className="px-6 py-6"><div className="mb-4 flex items-center justify-between"><div><p className="text-sm font-semibold">Form fields</p><p className="mt-1 text-xs text-muted-foreground">Drag order is represented with the arrow controls.</p></div><Button size="sm" variant="outline" onClick={() => setDraft((current) => [...current, { id: `custom-${Date.now()}`, label: "Custom question", type: "Text", enabled: true, required: false }])}><Plus />Add field</Button></div><div className="space-y-2">{draft.map((field, index) => <div key={field.id} className={cn("grid grid-cols-[auto_minmax(0,1fr)_auto] items-center gap-3 rounded-2xl border p-3", !field.enabled && "opacity-55")}><div className="flex flex-col"><button onClick={() => move(index, -1)} disabled={index === 0} className="text-muted-foreground disabled:opacity-20"><ArrowUp className="size-3.5" /></button><button onClick={() => move(index, 1)} disabled={index === draft.length - 1} className="text-muted-foreground disabled:opacity-20"><ArrowDown className="size-3.5" /></button></div><div className="min-w-0"><input value={field.label} onChange={(event) => update(field.id, "label", event.target.value)} className="w-full bg-transparent text-xs font-semibold outline-none" /><p className="mt-1 text-[10px] text-muted-foreground">{field.type}</p></div><div className="flex items-center gap-4"><label className="flex items-center gap-1.5 text-[10px] text-muted-foreground"><Checkbox checked={field.required} disabled={!field.enabled} onCheckedChange={(value) => update(field.id, "required", value === true)} />Required</label><Toggle checked={field.enabled} disabled={field.locked} onChange={(value) => update(field.id, "enabled", value)} /></div></div>)}</div></div><DialogFooter className="border-t bg-muted/20 px-6 py-4"><Button variant="outline" onClick={onClose}>Cancel</Button><Button onClick={save}><Check />Save fields</Button></DialogFooter></DialogContent></Dialog>;
}

function GoogleCalendarDialog({ open, onClose, integration, setIntegration }) {
  const [draft, setDraft] = useState(integration);
  const save = () => { setIntegration({ ...draft, connected: true, lastSync: "Just now" }); toast.success("Google Calendar connected"); onClose(); };
  return <Dialog open={open} onOpenChange={(next) => !next && onClose()}><DialogContent className="rounded-3xl p-0 sm:max-w-xl"><DialogHeader className="border-b bg-muted/30 px-6 py-6"><span className="mb-2 flex size-11 items-center justify-center rounded-2xl bg-blue-600 text-xl font-bold text-white">G</span><DialogTitle>{integration.connected ? "Manage Google Calendar" : "Connect Google Calendar"}</DialogTitle><DialogDescription>Prevent double bookings and add confirmed appointments to your calendar.</DialogDescription></DialogHeader><div className="space-y-5 px-6 py-6"><FloatingInput name="google-account" label="Google account" value={draft.account} onChange={(event) => setDraft((current) => ({ ...current, account: event.target.value }))} /><FloatingSelect label="Calendar" value={draft.calendar} displayValue={draft.calendar} onValueChange={(value) => setDraft((current) => ({ ...current, calendar: value }))}>{["Primary calendar", "Sales calls", "Product demos", "Customer onboarding"].map((item) => <SelectItem key={item} value={item}>{item}</SelectItem>)}</FloatingSelect>{[["checkConflicts", "Check calendar conflicts", "Hide slots that overlap with existing Google events."], ["createEvents", "Create calendar events", "Add confirmed bookings with guest details."], ["meetLink", "Add Google Meet link", "Automatically create video conferencing links."]].map(([key, label, help]) => <div key={key} className="flex items-start justify-between gap-5 rounded-2xl border p-4"><div><p className="text-sm font-semibold">{label}</p><p className="mt-1 text-xs text-muted-foreground">{help}</p></div><Toggle checked={draft[key]} onChange={(value) => setDraft((current) => ({ ...current, [key]: value }))} /></div>)}</div><DialogFooter className="border-t bg-muted/20 px-6 py-4"><Button variant="outline" onClick={onClose}>Cancel</Button><Button onClick={save} className="bg-blue-600 hover:bg-blue-700"><Link2 />{integration.connected ? "Save connection" : "Connect Google Calendar"}</Button></DialogFooter></DialogContent></Dialog>;
}

function SettingsTab({ serviceEnabled, setServiceEnabled, fields, setFields }) {
  const [fieldsOpen, setFieldsOpen] = useState(false);
  const [calendarOpen, setCalendarOpen] = useState(false);
  const [calendar, setCalendar] = useState({ connected: false, account: "shahtaz@atlas.co", calendar: "Primary calendar", checkConflicts: true, createEvents: true, meetLink: true, lastSync: "Never" });
  const [notifications, setNotifications] = useState({ confirmation: true, host: true, reminder24: true, reminder1: false, cancellation: true, followUp: false });
  const enabledFields = fields.filter((field) => field.enabled);
  return <div className="grid items-start gap-5 lg:grid-cols-2"><SectionCard icon={CalendarCheck2} title="Booking service" description="Enable or pause appointment booking without losing your setup." action={<Toggle checked={serviceEnabled} onChange={setServiceEnabled} />}><div className={cn("flex items-center gap-4 rounded-2xl border p-4", serviceEnabled ? "border-emerald-500/15 bg-emerald-500/[0.05]" : "bg-muted/30")}><span className={cn("flex size-10 items-center justify-center rounded-full", serviceEnabled ? "bg-emerald-500/10 text-emerald-600" : "bg-muted text-muted-foreground")}>{serviceEnabled ? <Check className="size-5" /> : <X className="size-5" />}</span><div><p className="text-sm font-semibold">{serviceEnabled ? "Booking is live" : "Booking is paused"}</p><p className="mt-1 text-xs text-muted-foreground">{serviceEnabled ? "Visitors can book available times through Atlas Support." : "The chatbot will not offer appointment slots."}</p></div></div></SectionCard><SectionCard icon={UserRoundCheck} title="Information to collect" description="Fields requested before an appointment is confirmed." action={<Button variant="ghost" size="icon-sm" onClick={() => setFieldsOpen(true)}><Pencil /></Button>}><div className="flex flex-wrap gap-2">{enabledFields.map((field) => <span key={field.id} className="rounded-full border px-2.5 py-1 text-[11px] font-medium">{field.label}{field.required && <span className="ml-1 text-red-500">*</span>}</span>)}</div><button onClick={() => setFieldsOpen(true)} className="mt-4 flex items-center gap-1 text-xs font-semibold text-primary">Configure fields <ChevronRight className="size-3.5" /></button></SectionCard><SectionCard icon={CalendarDays} title="Google Calendar" description="Sync bookings, prevent conflicts, and add Meet links." action={<span className={cn("rounded-full px-2.5 py-1 text-[10px] font-semibold", calendar.connected ? "bg-emerald-500/10 text-emerald-600" : "bg-muted text-muted-foreground")}>{calendar.connected ? "Connected" : "Not connected"}</span>}><div className="flex items-center gap-4 rounded-2xl border border-blue-500/15 bg-blue-500/[0.04] p-4"><span className="flex size-11 items-center justify-center rounded-2xl bg-blue-600 text-xl font-bold text-white">G</span><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{calendar.connected ? calendar.account : "Connect Google Calendar"}</p><p className="mt-1 text-xs text-muted-foreground">{calendar.connected ? `Primary sync · ${calendar.lastSync}` : "Availability, events, and Meet links"}</p></div></div><Button onClick={() => setCalendarOpen(true)} variant={calendar.connected ? "outline" : "default"} className={cn("mt-4 w-full", !calendar.connected && "bg-blue-600 hover:bg-blue-700")}><Link2 />{calendar.connected ? "Manage connection" : "Connect Google Calendar"}</Button></SectionCard><SectionCard icon={BellRing} title="Email notifications" description="Choose which booking emails Argon sends to guests and hosts."><div className="divide-y">{[["confirmation", "Guest confirmation", "Send date, time, timezone, and meeting link."], ["host", "Notify appointment host", "Email the assigned host when a booking is made."], ["reminder24", "24-hour reminder", "Remind the guest one day before the meeting."], ["reminder1", "1-hour reminder", "Send a final reminder shortly before the meeting."], ["cancellation", "Cancellation and reschedule", "Notify both guest and host about schedule changes."], ["followUp", "Post-meeting follow-up", "Send a follow-up email after completed meetings."]].map(([key, label, help]) => <div key={key} className="flex items-center justify-between gap-4 py-3"><div><p className="text-xs font-semibold">{label}</p><p className="mt-0.5 text-[11px] text-muted-foreground">{help}</p></div><Toggle checked={notifications[key]} onChange={(value) => setNotifications((current) => ({ ...current, [key]: value }))} /></div>)}</div><button className="mt-4 flex items-center gap-1 text-xs font-semibold text-primary">Edit email templates <ChevronRight className="size-3.5" /></button></SectionCard><SectionCard icon={CalendarX2} title="Cancellation policy" description="Give visitors clear options while protecting your team’s time." className="lg:col-span-2"><div className="grid gap-4 md:grid-cols-3">{[["Rescheduling", "Allowed up to 4 hours before"], ["Cancellation", "Allowed up to 2 hours before"], ["No-show handling", "Send host notification"]].map(([label, value]) => <button key={label} className="flex items-center justify-between rounded-2xl border p-4 text-left transition hover:bg-muted/40"><span><span className="block text-xs font-semibold">{label}</span><span className="mt-1 block text-[11px] text-muted-foreground">{value}</span></span><Pencil className="size-3.5 text-muted-foreground" /></button>)}</div></SectionCard><BookingFieldsDialog open={fieldsOpen} onClose={() => setFieldsOpen(false)} fields={fields} setFields={setFields} /><GoogleCalendarDialog open={calendarOpen} onClose={() => setCalendarOpen(false)} integration={calendar} setIntegration={setCalendar} /></div>;
}

const AppointmentBookingPage = () => {
  const [activeTab, setActiveTab] = useState("appointments");
  const [appointments, setAppointments] = useState(initialAppointments);
  const [schedule, setSchedule] = useState(initialSchedule);
  const [fields, setFields] = useState(initialBookingFields);
  const [serviceEnabled, setServiceEnabled] = useState(true);
  const [selected, setSelected] = useState(null);
  const updateStatus = (id, status) => { setAppointments((current) => current.map((item) => item.id === id ? { ...item, status } : item)); setSelected((current) => current?.id === id ? { ...current, status } : current); toast.success(`Appointment marked ${status.toLowerCase()}`); };
  const toggleService = (value) => { setServiceEnabled(value); toast.success(value ? "Appointment booking enabled" : "Appointment booking paused"); };
  return <section className="mx-auto max-w-7xl space-y-6 pb-8"><header className="flex flex-col gap-5 pr-14 xl:flex-row xl:items-end xl:justify-between"><div><Link to="/" className="mb-4 inline-flex items-center gap-2 text-sm font-medium text-muted-foreground transition hover:text-primary"><ArrowLeft className="size-4" />Back to workspace</Link><div className="flex items-center gap-4"><div className="flex size-14 shrink-0 items-center justify-center rounded-2xl bg-primary text-primary-foreground shadow-lg shadow-primary/20"><CalendarDays className="size-7" /></div><div><div className="flex items-center gap-2.5"><h1 className="text-2xl font-bold tracking-tight md:text-3xl">Appointment booking</h1><span className={cn("rounded-full px-2.5 py-1 text-[10px] font-semibold", serviceEnabled ? "bg-emerald-500/10 text-emerald-600" : "bg-muted text-muted-foreground")}>{serviceEnabled ? "Live" : "Paused"}</span></div><p className="mt-1 text-sm text-muted-foreground">Let visitors book the right teammate without leaving the conversation.</p></div></div></div><div className="flex items-center gap-3 rounded-full border bg-card px-4 py-2 shadow-sm"><div><p className="text-xs font-semibold">Accept bookings</p><p className="text-[10px] text-muted-foreground">{serviceEnabled ? "Service enabled" : "Service disabled"}</p></div><Toggle checked={serviceEnabled} onChange={toggleService} /></div></header><TabMenu tabs={pageTabs} activeTab={activeTab} setActiveTab={setActiveTab} scrollable className="sticky top-0 z-10 bg-background/95 backdrop-blur" />{activeTab === "appointments" && <AppointmentsTab appointments={appointments} setAppointments={setAppointments} openAppointment={setSelected} />}{activeTab === "availability" && <AvailabilityTab schedule={schedule} setSchedule={setSchedule} />}{activeTab === "settings" && <SettingsTab serviceEnabled={serviceEnabled} setServiceEnabled={toggleService} fields={fields} setFields={setFields} />}<AppointmentDialog appointment={selected} onClose={() => setSelected(null)} onStatusChange={updateStatus} /></section>;
};

export default AppointmentBookingPage;
